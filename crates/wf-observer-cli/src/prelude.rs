//! Common imports for the Warframe Observer CLI modules.

#[allow(
    unused_imports,
    reason = "the prelude intentionally offers every tracing level"
)]
pub(crate) use tracing::{debug, error, info, trace, warn};
