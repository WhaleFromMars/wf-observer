# Examples

Examples are grouped by language and then by framework or application type.

- `rust/dioxus` demos the full api.
- the rest of the examples exercise the minimal ffi bindings, connect to the service, ping it, and shutdown.
